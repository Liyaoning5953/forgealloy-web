# Public build-time values. Configure as GitHub repository variables before deployment.
# A GTM container wins when both are set; either one alone is enough.
VITE_GTM_ID=GTM-XXXXXXX
VITE_GA4_ID=G-XXXXXXXXXX
