# ForgeAlloy Forged Wheels Catalog

Premium Next.js 14+ catalog for forged wheels.

## 🚀 Deployment to GitHub Pages

This project is configured with GitHub Actions for automatic deployment.

1. Create a new repository on GitHub named `forgealloy-web`.
2. Run the following commands in this directory:

```bash
git init
git add .
git commit -m "Initial commit: ForgeAlloy Catalog"
git branch -M main
git remote add origin https://github.com/YOUR_USERNAME/forgealloy-web.git
git push -u origin main
```

3. Go to your GitHub Repository Settings -> Pages.
4. Under "Build and deployment" -> "Source", select **"GitHub Actions"**.
5. Your site will be live at `https://YOUR_USERNAME.github.io/forgealloy-web/`.

## 🛠 Features

- **Next.js 14 App Router**
- **Framer Motion** for smooth transitions
- **Tailwind CSS** for ultra-fast styling
- **Mobile Responsive**
- **Inquiry System**
- **WhatsApp Integration**
